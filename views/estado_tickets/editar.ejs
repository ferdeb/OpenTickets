<!DOCTYPE html>
<html>
<head>
  <title>Editar Estado Del Ticket</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="p-4">
  <h1>Editar Estado Del Ticket</h1>
  <form method="post" action="/estado_tickets/editar/<%= estado_ticket.estado_ticket_id %>">
    <input type="text" name="nombre" value="<%= estado_ticket.nombre %>" class="form-control mb-2">
    <select name="activo" class="form-control mb-3">
      <option value="1" <%= estado_ticket.activo ? 'selected' : '' %>>Activo</option>
      <option value="0" <%= !estado_ticket.activo ? 'selected' : '' %>>Inactivo</option>
    </select>
    <button class="btn btn-warning">Actualizar Estado Del Ticket</button>
  </form>
</body>
</html>