<!DOCTYPE html>
<html>
<head>
  <title>Editar Marca</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="p-4">
  <h1>Editar Marca</h1>
  <form method="post" action="/marcas/editar/<%= marca.marca_id %>">
    <input type="text" name="nombre" value="<%= marca.nombre %>" class="form-control mb-2">
    <select name="activo" class="form-control mb-3">
      <option value="1" <%= marca.activo ? 'selected' : '' %>>Activo</option>
      <option value="0" <%= !marca.activo ? 'selected' : '' %>>Inactivo</option>
    </select>
    <button class="btn btn-warning">Actualizar Marca</button>
  </form>
</body>
</html>