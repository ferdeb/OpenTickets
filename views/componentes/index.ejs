<!DOCTYPE html>
<html>
<head>
  <title>Componentes</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="p-4">
  <h1>Componentes</h1>
  <a href="/componentes/crear" class="btn btn-success mb-3">Registrar Nuevo Componente</a>
  <a href="/" class="btn btn-primary mb-3">Regresar al menu</a>
  <table class="table table-bordered">
    <thead>
      <tr>
        <th>Nombre</th>
        <th>Marca</th>
        <th>Precio Unitario</th>
        <th>Stock</th>
        <th>Modelo</th>
        <th>Articulo activo?</th>
        <th>Acciones</th>
      </tr>
    </thead>
    <tbody>
      <% componentes.forEach(a => { %>
        <tr>
          <td><%= a.nombre %></td>
          <td><%= a.marca %></td>
          <td><%= a.precio_unitario %></td>
          <td><%= a.stock %></td>
          <td><%= a.activo ? 'Sí' : 'No' %> </td>
          <!-- Aqui se emplean los alias -->
          <td>
            <a href="/componentes/editar/<%= a.componente_id %>" class="btn btn-warning btn-sm">Editar</a>
            <a href="/componentes/eliminar/<%= a.componente_id %>" class="btn btn-danger btn-sm" onclick="return confirm('¿Eliminar componente?')">Eliminar</a>
          </td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</body>
</html>