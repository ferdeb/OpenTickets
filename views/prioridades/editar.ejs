<!DOCTYPE html>
<html>
<head>
  <title>Editar prioridad</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="p-4">
  <h1>Editar prioridad</h1>
  <form method="post" action="/prioridades/editar/<%= prioridad.prioridad_id %>">
    <input type="text" name="nombre" value="<%= prioridad.nombre %>" class="form-control mb-2">
    <select name="activo" class="form-control mb-3">
      <option value="1" <%= prioridad.activo ? 'selected' : '' %>>Activo</option>
      <option value="0" <%= !prioridad.activo ? 'selected' : '' %>>Inactivo</option>
    </select>
    <button class="btn btn-warning">Actualizar</button>
  </form>
</body>
</html>