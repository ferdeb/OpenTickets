<!DOCTYPE html>
<html>
<head>
  <title>Editar Articulo Seleccionado</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="p-4">
  <h1>Editar Articulo Seleccionado</h1>
  <form method="post" action="/articulos/editar/<%= articulo.articulo_id %>">
    
    <!-- PARA EDITAR NO DEBO AGREGAR EL USUARIO, PUEDE DARSE A MAL USO -->
    <!-- DE DONDE TOMA EL ID ENTONCES? DEL BUTTON? -->
    <select name="modelo_id" class="form-control mb-3" required>
      <% modelos.forEach(u => { %>
        <option value="<%= u.modelo_id %>"><%= u.nombre %></option>
      <% }) %>
    </select>
 
    <input type="text" name="serie" value="<%= articulo.serie %>" class="form-control mb-2" required>
    
    <textarea name="descripcion" class="form-control mb-2"><%= articulo.descripcion %></textarea>
    
    <% let fecha_ingreso = articulo.fecha_ingreso ? new Date(articulo.fecha_ingreso).toISOString().slice(0, 10) : ''; %>
    <input type="date" name="fecha_ingreso" value="<%= fecha_ingreso %>" class="form-control mb-2">

    <select name="estado_articulo_id" class="form-control mb-3" required>
      <% estado_articulos.forEach(u => { %>
        <option value="<%= u.estado_articulo_id %>"><%= u.nombre %></option>
      <% }) %>
    </select>
    
    <button class="btn btn-warning">Actualizar Articulo</button>
  </form>
</body>
</html>